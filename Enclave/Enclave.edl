enclave
{
	from "sgx_pthread.edl" import *;
    from "sgx_tstdc.edl" import *;

	trusted
	{
		public void ecall_matprod([in, size=total_len]const int* a, [in, size=total_len]const int* b, [in, out, size=total_len]int* res, int total_len, int len);
	};
	untrusted
	{
	};
};
